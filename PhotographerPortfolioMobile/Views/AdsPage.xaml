<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PhotographerPortfolioMobile.Extensions"
             xmlns:viewModels="clr-namespace:PhotographerPortfolioMobile.ViewModels"
             xmlns:extensions="clr-namespace:PhotographerPortfolioMobile.Extensions"
             x:Class="PhotographerPortfolioMobile.Views.AdsPage"
             x:DataType="viewModels:AdsViewModel"
             BackgroundColor="{DynamicResource PageBackgroundColor}"
             Title="{extensions:Translate RemoveAds}">
    <VerticalStackLayout>
        <Label 
            Text="Ads Page"
            TextColor="{DynamicResource TextColor}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>